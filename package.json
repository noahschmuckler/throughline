{
  "name": "throughline",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Shared dyad project tracker — projects, dated entries, observations / decisions / actions / outcomes, with a dashboard, people view, and AI-assisted triage. Runs as a Cloudflare Worker (cloud demo) or a local Node server over a OneDrive-backed JSON file (orange-device production).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "start": "node server.js",
    "seed": "node scripts/seed.mjs",
    "test": "node --test test/"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@azure/msal-node": "^2.16.0",
    "turndown": "^7.2.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
