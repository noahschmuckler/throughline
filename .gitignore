node_modules/
# wrangler's lockfile — pins only the dev-only Cloudflare toolchain (no runtime
# deps ship; the Node server has none). Local-only, like node_modules/.
package-lock.json
.wrangler/
.dev.vars
.env
.env.local
.DS_Store
*.log
dist/

# Local Node-server state file (orange uses a OneDrive path instead).
/data/

# Local-only ticket scratchpad (issues/ideas/bugs from using the app).
/TICKETS.md
