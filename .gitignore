node_modules/
# package-lock.json is now TRACKED: the Node server gained runtime deps
# (@azure/msal-node + turndown for the Loop/Deloop Graph import), and the orange
# bundle vendors a prod-only `npm install` — so the lock must be reproducible.
.wrangler/
.dev.vars
.env
.env.local
.DS_Store
*.log
dist/
dist-package/

# Local Node-server state file (orange uses a OneDrive path instead).
/data/

# Local-only ticket scratchpad (issues/ideas/bugs from using the app).
/TICKETS.md
